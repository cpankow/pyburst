Welcome to PYBURST

pyburst is based on pycbc, more information can be found at the website:
   https://ldas-jobs.ligo.caltech.edu/~cbc/docs/pycbc/

When installing pyburst:
   python setup.py install --prefix=/path/to/install/directory

The most current development version is always available from our
git repository:

   git clone https://github.com/cpankow/pyburst.git

